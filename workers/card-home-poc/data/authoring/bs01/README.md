# BS01 赤カード原本（作業中）

編集元：bs01-red.authoring.json。ユーザー提供テキストと参考カード画像から転記。

これは公開マスターではありません。authoringSchemaVersionを持つ編集用原本です。既存のseed.mjsには直接渡さないでください。

- effectText：提供された原文を保持。効果なしカードは空文字。
- effectExecution：DISABLED。effectsは空配列。デメリットを含めて効果処理は実行しない方針。
- references：添付カード全体の原画像。イラスト単体の配信用画像ではない。
- illustration：未登録。代替画像を勝手に割り当てない。
- 公開前に、原本から既存ランタイムに適合する公開データを生成する。現在のランタイムはeffectTextが「効果なし」であることを要求するため、原文保持と実行無効を扱う公開手順を別途整備する。
- 現在28種類（スピリット19種類・ネクサス2種類・マジック7種類）。スピリットのみで40枚デッキを構成可能。DB公開は別途行う。

ローカルD1への登録・ビルド・テストは未実施。既存公開バージョンは変更しない。

## 追加分
BS01-012・013・014・015・016・018・019・020を追加。ふりがなはnameRubyに分離。効果は提供テキストを保持（参考画像にある細かな表現へ置き換えない）。

## ネクサスを含む追加分
BS01-021・022・024・025・099・101を追加。ネクサスはcardType=NEXUS、系統は空文字、BPフィールドは持たず、必要コア0／3で保存。複数系統は既存のtribe表示文字列「竜人・古竜」として保持。効果処理は無効。現在のスピリット専用ランタイムへのネクサス公開は行わない。

## マジック追加
BS01-115・116・117・118・119・120・122を追加。マジックはlevelsとsymbolsを空配列、系統は空文字で保持。BPや維持コアの値は作らない。

BS01-116は提供文BP+5000／参考画像BP+2000の不一致があるためneeds-confirmation。提供文を暫定保持し、確定扱いにしない。効果実行は全カードDISABLED。

## ゴラドン追加
BS01-001：コスト0、軽減0、爬獣、Lv1は1コア／BP1000、Lv2は3コア／BP3000、赤シンボル1。効果なし。当初指定の28種類が揃った。BS01-116の効果数値確認は継続。
